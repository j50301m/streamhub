apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: api-env
spec:
  data:
    - secretKey: database_url
      remoteRef:
        key: streamhub-staging-api-database-url
    - secretKey: redis_url
      remoteRef:
        key: streamhub-staging-redis-url
    - secretKey: jwt_secret
      remoteRef:
        key: streamhub-staging-jwt-secret
    - secretKey: gcs_bucket
      remoteRef:
        key: streamhub-staging-gcs-bucket
    - secretKey: transcoder_project_id
      remoteRef:
        key: streamhub-staging-transcoder-project-id
    - secretKey: pubsub_verify_token
      remoteRef:
        key: streamhub-staging-pubsub-verify-token
