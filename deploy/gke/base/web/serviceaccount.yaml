apiVersion: v1
kind: ServiceAccount
metadata:
  name: web
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: streamhub
# No Workload Identity annotation: the web container only serves static assets
# through nginx and does not need to call any GCP API.
